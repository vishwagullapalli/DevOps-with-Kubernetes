apiVersion: v1
kind: Service
metadata:
    name: todo-app-srvc
spec:
    type: NodePort
    selector:
        app: todo-app
    ports:
        - name: http
          nodePort: 30010
          protocol: TCP
          port: 8765
          targetPort: 9876